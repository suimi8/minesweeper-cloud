# Linuxdo Mall 后台说明

当前项目已从原版 Flask + JSON 文件后台迁移为 Cloudflare Pages + Worker + D1，主线以商城为中心，扫雷仅作为娱乐活动模块保留。

## 已迁移模块

- 运营概览：用户、商品、订单、成交额、卡密、优惠码、评价、低库存和登录风险摘要。
- 商品管理：发布新项目、上下架、售罄分组、删除分组、批量上架、批量下架、批量删除。
- 商品发布字段：商品编号自动生成、首页图片、详情页轮播图每行一个 URL、官方同步 Token、库存阈值、限购、最低信任等级、亮点、售后说明、买家信息字段 JSON、手动库存、自动卡密库存。
- 卡密管理：批量导入、定时上架、编辑、删除、状态筛选。
- 订单管理：交付、取消、退款标记、批量删除、买家信息查看、订单 JSON 导出。
- 优惠码管理：固定抵扣、百分比、限量、有效期、限制商品、启停、批量删除。
- 优惠码批量生成：支持前缀、数量、面值、商品限制和有效期。
- 评价管理：查看和批量删除评价。
- 广告推荐：新增、编辑、位置、排序、启停、批量删除。
- 邮件模板：新增、编辑、默认模板、预览、删除。模板数据已保存在 D1。
- 风控黑名单：IP/用户规则新增、删除、批量删除，并已接入 API 拦截。
- 用户管理：Linux.do 用户信息、订单数、消费额、批量删除。
- 站点设置：站点信息、公告、抽奖、Resend 邮件配置说明、审核词、备份配置。
- 登录记录：Linux.do 和本地登录记录，含成功/失败、IP、User-Agent。
- 备份导出：导出 D1 JSON 快照，记录导出历史。
- 数据分析：订单趋势、订单状态、热销商品、高价值用户。
- 系统全景监控：用 D1 中的订单、会话、登录记录和审计日志替代原版 Redis God View，展示运行状态、风险登录、低库存和实时事件流。
- 运维工具：卡密状态修复、重复卡密分析/清理、订单 CSV 导出、用户 CSV 导出、审计日志清空、浏览器本地水印取证工具。
- 扫雷管理：扫雷排行榜、用户、存档等娱乐模块独立管理。
- Linux.do 信任等级：登录时保存 TL 等级，商品最低信任等级会在下单时强制校验。

## 和原版不同的地方

- 原版的服务器 ZIP 备份依赖 Flask 文件系统和 Celery。Cloudflare Pages 没有持久文件系统，所以改为 D1 JSON 快照导出。
- 原版邮件客户端直连由 Flask 执行。Cloudflare 版改为 Resend HTTPS API，后台邮箱配置页只展示 Resend 配置步骤和测试发件入口。
- 原版管理员角色系统依赖站点账号。当前仍以 Linux.do 超级管理员为入口：`suimi` / Linux.do ID `126431`。
- 原版 God View 的 QPS 来自 Redis 请求埋点。Cloudflare 版没有 Redis，因此改为基于 D1 近 5 分钟后台事件估算后台事件速率。
- 原版 Excel 导出依赖 openpyxl。Cloudflare 版改为 CSV 导出，可直接用 Excel/WPS 打开。

## 数据持久化

商城、扫雷、后台设置、黑名单、邮件模板、登录记录和备份记录都保存在 Cloudflare D1 `minesweeper-cloud`，不会因为刷新页面或重新部署而丢失。
